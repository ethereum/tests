{
    "net_peerCount" : {
            "request_method": "net_peerCount",
            "request_params": [
        ],
    "expect_return" : {
            "type": "string",
            "enum": ["0x0"]
        }
    }
}
